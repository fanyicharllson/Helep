apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: sos-service-hpa
  namespace: {{ .Release.Namespace }}
  labels:
    app: sos-service
    app.kubernetes.io/part-of: helep
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: sos-service
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
