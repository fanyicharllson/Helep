apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-sos-service-secret
  namespace: {{ .Release.Namespace }}
  labels:
    app: sos-service
    app.kubernetes.io/part-of: helep
type: Opaque
stringData:
  JWT_SECRET: {{ .Values.jwtSecret | default .Values.global.jwtSecret | default "change-me-in-production" | quote }}
